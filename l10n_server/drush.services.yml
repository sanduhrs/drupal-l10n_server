services:
  l10n_server.commands:
    class: \Drupal\l10n_server\Commands\L10nServerCommands
    arguments: ['@config.factory', '@database', '@queue', '@entity_type.manager', '@datetime.time', '@plugin.manager.l10n_server.connector']
    tags:
      - { name: drush.command }
